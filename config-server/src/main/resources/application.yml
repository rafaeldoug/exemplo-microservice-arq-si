server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          search-paths:          - config-repo          
          uri: https://github.com/rafaeldoug/exemplo-microservice-arq-si/
          default-label: main
